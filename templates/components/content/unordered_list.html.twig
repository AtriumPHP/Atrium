{# Unordered list content: a bulleted list of plain-text items. #}
<ul class="list-disc space-y-1 pl-5 text-sm text-gray-600 dark:text-gray-400">
    {% for item in component.getItems() %}
        <li>{{ item }}</li>
    {% endfor %}
</ul>
