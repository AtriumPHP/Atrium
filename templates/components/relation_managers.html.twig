{% if this.hasRelations %}
    <div {{ attributes.defaults({ class: 'mt-8 space-y-8' }) }}>
        {% for relation in this.getRelations %}
            <section class="space-y-3">
                <h2 class="flex items-center gap-2 text-sm font-semibold text-gray-700 dark:text-gray-200">
                    {% if relation.icon %}{{ atrium_icon(relation.icon, { class: 'h-4 w-4' }) }}{% endif %}
                    {{ relation.label }}
                </h2>
                {# REL-M3: when this host renders multiple managers as a tab strip,
                   give each a stable key ("{parentSlug}:{parentId}:{relation}") so
                   they keep page/search/sort state across tab switches. With a
                   single section (M1) positional keying is sufficient. #}
                {{ component('Atrium:RelationManager', {
                    resource: resource,
                    parentId: parentId,
                    relation: relation.name,
                    pathPrefix: pathPrefix,
                    screen: screen,
                }) }}
            </section>
        {% endfor %}
    </div>
{% endif %}
