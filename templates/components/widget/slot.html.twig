{# A widget placed in a dashboard layout (WidgetSlot leaf): renders the
   independent live host for the wrapped widget class. `component` is the slot. #}
<twig:Atrium:Widget :widget="component.getWidgetClass()" />
